cmake -S . -B build -G "MinGW Makefiles" -DCMAKE_BUILD_TYPE=Release #Release or Debug; Only necessary once

cmake --build build --parallel 4 #Just --parallel if we want to use all threads; This is the compilation phase